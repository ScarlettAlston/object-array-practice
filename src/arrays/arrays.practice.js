const arraysSeed = require("./arrays.seed");
let numberArray = arraysSeed.numberArray;
let letterArray = arraysSeed.letterArray;
let wordArray = arraysSeed.wordArray;
let objectArray = arraysSeed.objectArray;

// By default, find these arrays at localhost:5000/arrays

// Modify the number array
function modifyNumberArray() {
  return;
}

// Modify the letter array
function modifyLetterArray() {
  return;
}

// Modify the word array
function modifyWordArray() {
  return;
}

// Modify the object array
function modifyObjectArray() {
  return;
}

// Run practice functions
modifyNumberArray();
modifyLetterArray();
modifyWordArray();
modifyObjectArray();

module.exports = {
  numberArray,
  letterArray,
  wordArray,
  objectArray
}